# code-rl-pipeline — pip deps (mirror of scripts/setup_env.sh)
# Target: RTX 5090 32 GB (Blackwell sm_120) · CUDA 13.0 · Python 3.11
# Validated 2026-05-19 — see results/final_summary.md
#
# NOTE: there are TWO env stacks in this project, kept separate for ABI reasons:
#
#   coderl      — vllm + transformers 5.8 + torch 2.11+cu130 (this file)
#   coderl_sft  — unsloth + trl 0.24 + transformers 5.5 + torch 2.10+cu128
#                 (bootstrap: `bash scripts/setup_env_sft.sh`)
#
# Always prefer the setup_env.sh / setup_env_sft.sh scripts — they handle
# CUDA toolkit install, header/lib symlinks, transformers patches.
# This requirements.txt is for reference only.
#
# torch — pypi default in 2026-05 ships +cu130 build, which is what vllm 0.21
# expects (libcudart.so.13). Do NOT use --index-url cu129 (gives wrong wheel).

torch==2.11.0
torchvision==0.26.0

transformers>=5.8,<5.9
accelerate>=0.34
datasets>=4.0
peft>=0.14
sentencepiece
protobuf
safetensors

# vllm 0.21 structured-output deps — looser pins get yanked at install time
vllm>=0.21,<0.22
xgrammar>=0.2.0
outlines_core==0.2.14
llguidance>=1.3.0,<1.4.0

bitsandbytes>=0.49
autoawq>=0.2.7

wandb
tqdm
pyyaml
jsonlines
pandas
numpy
pyarrow

# === coderl_sft env (DO NOT install in coderl) ===
# unsloth         # 2026.5.4+ for Blackwell sm_120
# unsloth_zoo     # pulled by unsloth, pins torch 2.10 — incompatible with vllm 0.21
# trl>=0.24       # for SFT/DPO/GRPO trainers (NB: requires mergekit, llm-blender, weave; trl 0.24 also has TRANSFORMERS_CACHE import in llm_blender that needs patching for transformers 5.5+)
